CREATE TABLE IF NOT EXISTS foodEntity (
    id TEXT NOT NULL PRIMARY KEY,
    user_id TEXT NOT NULL,
    name TEXT NOT NULL,
    carbs REAL NOT NULL,
    proteins REAL NOT NULL,
    fats REAL NOT NULL,
    calories REAL NOT NULL,
    size REAL NOT NULL,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);
